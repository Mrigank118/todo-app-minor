apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-todo-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: simple-todo
  template:
    metadata:
      labels:
        app: simple-todo
    spec:
      containers:
      - name: simple-todo
        image: jainritikaa05/todo-app:latest
        ports:
        - containerPort: 80
